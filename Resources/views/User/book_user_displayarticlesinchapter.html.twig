{{ pageSetVar('title', chapter.name) }}
<h2>{{ __('Chapter') }}{{ chapter.number }} - {{ chapter.name }}</h2>
{% for article in articles %}
<h3>{{ chapter.number }} - {{ article.number }}  {{ article.title }}</h3> 
{{ article.contents|raw }}
<hr />
{% endfor %}
