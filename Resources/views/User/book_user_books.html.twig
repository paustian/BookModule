{{ pageSetVar('title', 'The Book List') }}
<div class="container-fluid">
    <h3>{{ 'Available Books on this Site' | trans  }}</h3>
    {% for item in books %}
        <p><a href="{{ path('paustianbookmodule_user_toc') }}/{{ item.bid }}" class="fa fa-book"> {{ item.name }}</a></p>
    {% endfor %}
        
</div>